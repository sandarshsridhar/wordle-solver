{
  "name": "wordle-solver",
  "version": "1.0.0",
  "type": "module",
  "description": "code that solves highly popular game called 'wordle'",
  "main": "wordle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && npm i && tsc",
    "start": "tsc && node dist/wordle.js"
  },
  "author": "Sandarsh Sridhar",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "@types/prompt-sync": "^4.1.1"
  },
  "dependencies": {
    "chalk": "^5.0.0",
    "prompt-sync": "^4.1.6"
  }
}